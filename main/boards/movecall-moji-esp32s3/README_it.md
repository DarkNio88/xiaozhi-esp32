# Movecall Moji — Guida di compilazione

Impostare il target su `ESP32S3` e selezionare la board in `menuconfig`:

```bash
idf.py set-target esp32s3
idf.py menuconfig
# Xiaozhi Assistant -> Board Type -> Movecall Moji 小智AI衍生版
idf.py build
```
